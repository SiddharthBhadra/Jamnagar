<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jamnagar">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Refinery"></activity>
        <activity android:name=".Temples" />
        <activity android:name=".Food" />
        <activity android:name=".Army" />
        <activity android:name=".army" />
        <activity android:name=".Hotels" />
        <activity android:name=".History" />
        <activity android:name=".Places" />
        <activity android:name=".Categorys_jamnagar" />
        <activity android:name=".Category" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>